---
- name: Configure nginx web server
  hosts: worker
  become: yes
  tasks:
  - name: uninstall nginx server
    apt:
      name: nginx=1.18*
      state: absent
  - name: stop nginx server
    service:
      name: nginx
      state: stopped

